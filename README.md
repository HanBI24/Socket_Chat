# Socket Chat
## **For study**  

**Client :** Android (Kotlin) + nkzawa (socket.io-client)  

**Server :** Express + Socket.io
```
"express": "^4.17.1"
"socket.io": "^3.1.1"
```

**Start to server** (port 5000)
```
npm run start
```